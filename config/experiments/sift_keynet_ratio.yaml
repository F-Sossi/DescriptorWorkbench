experiment:
  name: "sift_keynet_ratio"
  description: "SIFT descriptor evaluated on Kornia KeyNet keypoints with ratio-test matching"
  version: "1.0"
  author: "researcher"

dataset:
  type: "hpatches"
  path: "../data/"
  scenes: []

keypoints:
  generator: "keynet"
  max_features: 2000
  source: "independent_detection"
  keypoint_set_name: "keynet_kornia_shared"

descriptors:
  - name: "sift_keynet_ratio"
    type: "sift"
    pooling: "none"
    normalize_after_pooling: false

evaluation:
  matching:
    method: "ratio_test"
    ratio_threshold: 0.8
    norm: "l2"
    cross_check: false

  validation:
    method: "homography"
    threshold: 0.05
    min_matches: 10

output:
  results_path: "results/"
  save_keypoints: false
  save_descriptors: false
  save_matches: false
  save_visualizations: false

database:
  enabled: true
  connection: "sqlite:///experiments.db"
