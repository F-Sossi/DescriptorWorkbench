experiment:
  name: "libtorch_sosnet_keynet_ratio"
  description: "LibTorch SOSNet descriptor evaluated on KeyNet keypoints with ratio-test matching"
  version: "1.0"
  author: "researcher"

dataset:
  type: "hpatches"
  path: "../data/"
  scenes: []  # All scenes

keypoints:
  generator: "keynet"
  max_features: 2000
  source: "independent_detection"
  keypoint_set_name: "keynet_kornia_shared"

descriptors:
  - name: "libtorch_sosnet_keynet_ratio"
    type: "libtorch_sosnet"
    pooling: "none"
    normalize_after_pooling: true

evaluation:
  matching:
    method: "ratio_test"
    ratio_threshold: 0.8
    norm: "l2"
    cross_check: false

  validation:
    method: "homography"
    threshold: 0.05
    min_matches: 10

output:
  results_path: "results/"
  save_keypoints: false
  save_descriptors: false
  save_matches: false
  save_visualizations: false

database:
  enabled: true
  connection: "sqlite:///experiments.db"
